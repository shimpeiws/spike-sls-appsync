type Branch {
  id: ID!
  companyID: ID!
  name: String!
  createdAt: AWSDateTime!
}

input CreateInputBranch {
  name: String!
}

type Query {
  listBranch: [Branch!]!
}

type Mutation {
  createBranch(input: CreateInputBranch!): Branch
}
